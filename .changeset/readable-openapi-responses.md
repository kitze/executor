---
"@executor-js/plugin-openapi": patch
---

Keep request-only fields and public recursive schemas from making otherwise readable OpenAPI responses opaque, while preserving protection for actual sensitive fields.
