---
"@executor-js/sdk": patch
"@executor-js/api": patch
"@executor-js/host-selfhost": patch
---

Preserve registered OAuth callbacks across dashboard aliases, support an explicit
origin allowlist for private HTTP OAuth providers, and expose fresh connection
health checks to agents.
